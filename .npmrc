# Always install devDependencies (Render sets NODE_ENV=production, which would
# otherwise skip the typescript/@types/prisma/vite needed to build).
include=dev
# Match our install workflow; RN's peer-dependency graph needs this to resolve.
legacy-peer-deps=true
